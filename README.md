# 📝 Memo App Project

## 📂 1. API 파일 구조 및 역할

- **`client.js` (Axios)**:
  - API 통신의 기초가 되는 공통 설정을 관리
  - BaseURL, Timeout, Headers 등을 설정
- **`memos.js` (API)**:
  - 직접적으로 서버의 데이터를 불러오거나 보내는 역할을 하는 함수들이 모여있는 유틸 파일
  - `getMemos`, `createMemo`, `updateMemo`, `deleteMemo` 함수들로 서버와의 실제 테이터 교환을 담당합니다.

---

## 🎨 2. 상태별 UI 처리 로직

저는 커스텀 훅 안에서 모든 상태 UI에 대한 상태를 관리하고 있습니다<br/>
그래서 서버에서 클라이언트로 데이터를 불러오고, 불러온 데이터가 비어 있거나 통신 에러로 불러오지 못하면<br/>
상태 UI의 상태가 true가 되며 사용자에게 현재 상태를 알리는 메시지 컴포넌트를 렌더링합니다.

| 상태 UI     | 노출 조건            | 역할                                                                                                  |
| :---------- | :------------------- | :---------------------------------------------------------------------------------------------------- |
| **Loading** | `isLoading === true` | 데이터를 불러오는 중임을 알리는 스피너 컴포넌트를 활성화합니다.                                       |
| **Error**   | `isError === true`   | 서버 통신 실패(500 에러 등) 시 안내 컴포넌트를 활성화합니다.                                          |
| **Empty**   | `isEmpty === true`   | 불러온 데이터가 비어 있거나 작성한 메모가 <br/>없으면 작성을 유도하는 메시지 컴포넌트가 활성화됩니다. |

---

## 🛠 3. CRUD 구현 설명

- **Create**: 새로운 메모를 작성하면 서버 저장 후, 서버에서 포맷팅된 정보를 불러와 `useSyncMemos.js`에 보내 화면에 렌더링 <br/>🔍 : `const createMemoSync = (newMemo) => setMemos((prev) => [newMemo, ...prev]);`
- **Update**: 수정 모드에서 Spread(`...`)를 사용하여 기존 객체의 데이터를 안전하게 복사하고, 변경된 내용만 새 객체로 교체하여 렌더링합니다.
  <br/>🔍 : `const updateMemoSync = (memo) =>
setMemos((prev) => prev.map((m) => (m.id === memo.id ? memo : m)));`
- **Delete**: `filter` 메서드를 사용하여 해당 ID를 제외한 나머지 메모들로 상태를 즉시 갱신합니다. <br/>🔍 : `const deleteMemoSync = (id) =>
setMemos((prev) => prev.filter((m) => m.id !== id));`

---

## 🔄 4. 선택 확장 과제(재시도 버튼)

- 통신 에러로 인해 데이터 불러오기에 실패 했을 시 메시지 옆에 재시도 버튼을 눌러 서버 다시 요청을 보냅니다.

---
